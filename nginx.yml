apiVersion: v1
kind: Pod
metadata:
   name: nginx-pod
   labels:
      app: nginxapp
spec:
    containers:
       - name: nginx-container
         image: nginx
         ports:
          - name: nginx-port
           containerPort: 80
