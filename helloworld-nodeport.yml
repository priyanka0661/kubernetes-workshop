apiVersion: v1
kind: Service
metadata:
  name: helloworldservice
spec:
  ports:
    - port: 31001
      nodePort: 31001
      targetPort: nodejs-port
      protocol: tcp
   selector:
     app: helloworld
   type: NodePort
